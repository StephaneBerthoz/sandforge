---
wave: 1
depends_on: []
files_modified:
  - knip.json
  - .github/workflows/knip.yml
  - package.json
  - packages/extension/package.json
  - packages/webview/package.json
  - packages/shared/package.json
autonomous: true
---

# Plan 01-02: Knip CI + Dead Code Cleanup

**Requirements covered:** HARD-04 (Knip in CI + initial cleanup pass)

**Goal:** Install Knip with monorepo-aware config, add non-blocking CI workflow publishing findings as artifact, execute initial cleanup pass (delete unused files, unused exports, unused dependencies) with one commit per category for reviewability.

**Non-goals:** Making Knip a blocking check (deferred to v1.4), deep refactors (this is pure delete-pass).

## Tasks

### Task 01-02-01: Install Knip + ts-morph
<task>
<file>package.json</file>
<action>Add to devDependencies at repo root: `knip ^5.x`, `ts-morph ^23.x` (ts-morph used later by Plan 01-04). Run `pnpm install`.</action>
<verify>`pnpm list knip ts-morph` shows both installed. `pnpm typecheck` green.</verify>
<done>Both dev deps installed, no peer warnings.</done>
</task>

### Task 01-02-02: Knip config
<task>
<file>knip.json</file>
<action>Create at repo root with monorepo workspace config: `{ "$schema": "https://unpkg.com/knip@5/schema.json", "workspaces": { "packages/shared": { "entry": ["src/index.ts"] }, "packages/extension": { "entry": ["src/extension.ts"], "ignoreBinaries": ["esbuild"] }, "packages/webview": { "entry": ["src/main.tsx", "src/PanelApp.tsx"], "project": ["src/**/*.{ts,tsx}"] } }, "ignoreDependencies": ["@types/.*"] }`. Add `"knip": "knip"` script to root package.json.</action>
<verify>`pnpm knip` runs without config errors (may report findings — that's fine, we clean up next tasks).</verify>
<done>`pnpm knip` runs to completion, emits report.</done>
</task>

### Task 01-02-03: Knip CI workflow
<task>
<file>.github/workflows/knip.yml</file>
<action>Create non-blocking workflow: `name: Knip / on: [pull_request, push] / jobs: knip / runs-on: ubuntu-latest / steps: checkout, setup pnpm, install, run `pnpm knip --reporter markdown > knip-report.md || true`, upload artifact knip-report.md`. Does NOT fail CI — purely informational. Comments results on PR via `actions/github-script` reading artifact.</action>
<verify>Workflow YAML valid (run `actionlint` if available, else manual review). Does not set `exit 1` on knip findings.</verify>
<done>Workflow is non-blocking, publishes artifact on every run.</done>
</task>

### Task 01-02-04: Initial cleanup — unused exports
<task>
<file>packages/*/src/**/*.ts</file>
<action>Run `pnpm knip --include exports,types --reporter compact`, triage flagged unused exports. Remove items that are clearly internal-only. KEEP: anything exported from package barrel `index.ts` (may be external API), anything with `@public` JSDoc, anything re-exported by `@sandforge/shared`. Commit via task executor as `chore(knip): remove N unused exports`.</action>
<verify>`pnpm knip --include exports` reports ≥ 50% fewer flagged exports than baseline. `pnpm test` unchanged (no regressions).</verify>
<done>Unused exports down significantly, no test regressions.</done>
</task>

### Task 01-02-05: Initial cleanup — unused files
<task>
<file>packages/*/src/**/*.ts</file>
<action>Run `pnpm knip --include files --reporter compact`. Review each file — if it's truly orphan (no imports anywhere), delete it + its `.test.ts` sibling. KEEP: `*.d.ts` type augmentations, `*.config.ts` even if not imported (may be tooling config), anything referenced by `package.json` scripts.</action>
<verify>No broken imports after deletes. `pnpm typecheck` green. `pnpm test` green. Test count either stays or drops by exactly the test count of deleted test files.</verify>
<done>Orphan files removed, build still green.</done>
</task>

### Task 01-02-06: Initial cleanup — unused dependencies
<task>
<file>package.json (root + 3 packages)</file>
<action>Run `pnpm knip --include dependencies,devDependencies --reporter compact`. For each flagged dep: verify it's not used by any script in package.json, not a peer dep requirement, not a transitive requirement. Remove via `pnpm remove <pkg> --filter <workspace>`. Commit as `chore(knip): remove N unused dependencies`.</action>
<verify>`pnpm install` clean. `pnpm build && pnpm test` green.</verify>
<done>Unused deps removed, build + tests green.</done>
</task>

### Task 01-02-07: Baseline report committed
<task>
<file>.planning/phases/01-hardening-foundations/01-02-KNIP-BASELINE.md</file>
<action>Save `pnpm knip --reporter markdown` output to this file as the post-cleanup baseline. Document count of remaining flagged items (acceptable at this point) so future milestones can trend.</action>
<verify>File exists with knip output.</verify>
<done>Baseline captured for future comparison.</done>
</task>

## Must-Haves

- [ ] `knip` and `ts-morph` installed as devDependencies at root
- [ ] `knip.json` at repo root with valid monorepo workspace config
- [ ] `pnpm knip` script works, runs to completion
- [ ] `.github/workflows/knip.yml` exists, non-blocking, publishes report as artifact
- [ ] Unused exports cleanup PR committed (≥ 50% reduction from baseline)
- [ ] Unused files cleanup PR committed (orphans deleted)
- [ ] Unused dependencies cleanup PR committed
- [ ] `pnpm typecheck && pnpm test && pnpm build` all green post-cleanup
- [ ] Post-cleanup Knip baseline report saved to `01-02-KNIP-BASELINE.md`
- [ ] Test count either stays at baseline (8320) or drops only by deleted orphan test counts
