---
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-monitor-enrichment/05-BENCHMARK.md
  - packages/extension/src/bridge/handlers/MonitorOpsHandler.ts
  - packages/extension/src/bridge/handlers/MonitorOpsHandler.test.ts
  - packages/extension/src/bridge/ExtensionHandlers.ts
  - packages/webview/src/pages/Monitor/StorageBreakdownPanel.tsx
  - packages/webview/src/pages/Monitor/StorageBreakdownPanel.test.tsx
  - packages/webview/src/pages/Monitor/DeploymentTimeline.tsx
  - packages/webview/src/pages/Monitor/DeploymentTimeline.test.tsx
  - packages/webview/src/pages/Monitor/LimitExportButton.tsx
  - packages/webview/src/pages/Monitor/LimitExportButton.test.tsx
  - packages/webview/src/pages/Monitor/ApiUsagePanel.tsx
  - packages/webview/src/pages/Monitor/ApiUsagePanel.test.tsx
  - packages/webview/src/pages/Monitor/MonitorPage.tsx
  - packages/webview/src/pages/Monitor/MonitorPage.test.tsx
  - packages/webview/src/pages/Monitor/useMonitorPageData.ts
  - packages/webview/src/i18n/locales/en.json
  - packages/webview/src/i18n/locales/fr.json
  - packages/shared/src/types/messages.types.ts
autonomous: true
objective: "Competitor benchmark + 5 feature gaps: storage breakdown, deployment timeline, limit export, API analytics, health-score fix"
must_haves:
  - "Competitor benchmark documented in 05-BENCHMARK.md"
  - "StorageBreakdownPanel shows per-object storage with donut chart"
  - "DeploymentTimeline shows recent deployments from DeploymentTracker"
  - "LimitExportButton exports trend data as CSV"
  - "ApiUsagePanel shows per-category API usage breakdown"
  - "monitor:health-score handler route registered and working"
  - "New panels integrated into MonitorPage layout"
  - "All new components have tests"
  - "i18n keys in en.json and fr.json"
  - "pnpm typecheck passes"
  - "pnpm test passes"
---

# Plan 01: Feature Gaps Implementation

## Objective

Document competitor benchmark (MON-01), then implement the top 5 feature gaps (MON-02): storage breakdown panel, deployment timeline, limit history export, API usage analytics, and health-score handler fix.

## Context

- Most backend infrastructure exists (DeploymentTracker, TrendStorage, sforceLimitParser)
- This plan creates new UI panels and wires them to existing backends
- Recharts already used for area charts (use PieChart for storage donut)
- Timeline component exists for deployment display
- MonitorOpsHandler needs new message handlers for storage/deployments/api-usage

## Tasks

<task id="05-01-01">
<name>Write competitor benchmark document</name>
<files>
- .planning/phases/05-monitor-enrichment/05-BENCHMARK.md
</files>
<action>
Create a structured comparison document:

```markdown
# Monitor Competitor Benchmark

## Competitors Analyzed
1. Salesforce Inspector (Chrome Extension)
2. ORGanizer for Salesforce (Free app)
3. Salesforce Org Monitor (Managed package)
4. DevOps Center (Native Salesforce)

## Feature Matrix
| Feature | SF Inspector | ORGanizer | Org Monitor | SandForge (Before) | SandForge (After) |
|---------|-------------|-----------|-------------|--------------------|--------------------|
| Governor Limits | ... | ... | ... | ... | ... |
| Storage Breakdown | ... | ... | ... | ... | ... |
| ...
```

Cover: limits, health, storage, deployments, API profiling, alerts, jobs, audit trail, multi-org, export.
Mark what SandForge has now vs what Phase 5 adds.
</action>
<verify>
- 05-BENCHMARK.md exists with complete comparison
- All 4 competitors covered
</verify>
<done>[ ]</done>
</task>

<task id="05-01-02">
<name>Add storage, deployment, API usage handlers to MonitorOpsHandler</name>
<files>
- packages/extension/src/bridge/handlers/MonitorOpsHandler.ts
- packages/extension/src/bridge/handlers/MonitorOpsHandler.test.ts
- packages/extension/src/bridge/ExtensionHandlers.ts
- packages/shared/src/types/messages.types.ts
</files>
<action>
Read MonitorOpsHandler.ts and ExtensionHandlers.ts.

1. Add new message type definitions to messages.types.ts:
   - `monitor:storage` → `monitor:storage:response` (per-object storage data)
   - `monitor:deployments` → `monitor:deployments:response` (recent deployments)
   - `monitor:api-usage` → `monitor:api-usage:response` (API category breakdown)

2. Add 3 new handler methods to MonitorOpsHandler:

**handleStorage:** Query EntityDefinition or use describe results to estimate per-object record counts. Return top 20 objects by count with labels.

**handleDeployments:** Use existing DeploymentTracker.getHistory() if available, or query `SELECT Id, Status, StartDate, CompletedDate, CreatedBy.Name FROM DeployRequest ORDER BY StartDate DESC LIMIT 20`. Return deployment list.

**handleApiUsage:** Use existing sforceLimitParser data or query limits endpoint. Break down by category (DailyApiRequests, DailyBulkApiRequests, DailyStreamingApiEvents, etc.). Return category name, max, used, percentage.

3. Register monitor:health-score route in ExtensionHandlers.ts (fix pending todo).

4. Register new message types in ExtensionHandlers.

Tests:
- handleStorage returns object breakdown
- handleDeployments returns deployment list
- handleApiUsage returns category breakdown
- monitor:health-score route registered
</action>
<verify>
- 3 new handler methods in MonitorOpsHandler
- monitor:health-score routed
- Types defined in messages.types.ts
- Tests pass
</verify>
<done>[ ]</done>
</task>

<task id="05-01-03">
<name>Create StorageBreakdownPanel and ApiUsagePanel</name>
<files>
- packages/webview/src/pages/Monitor/StorageBreakdownPanel.tsx
- packages/webview/src/pages/Monitor/StorageBreakdownPanel.test.tsx
- packages/webview/src/pages/Monitor/ApiUsagePanel.tsx
- packages/webview/src/pages/Monitor/ApiUsagePanel.test.tsx
</files>
<action>
**StorageBreakdownPanel:**
- Fetch data via useBridgeQuery('monitor:storage')
- Recharts PieChart (donut variant) showing top 10 objects by record count
- Table below with all objects: name, record count, percentage
- Color coding: largest objects get distinct colors
- Loading skeleton while fetching
- Empty state if no data

**ApiUsagePanel:**
- Fetch data via useBridgeQuery('monitor:api-usage')
- Table with columns: Category, Used, Max, Usage %
- ProgressBar for each category's usage percentage
- Sort by usage % descending (most consumed first)
- Badge for categories > 80% usage (warning) or > 95% (critical)
- Loading skeleton

Both panels: use Card wrapper, i18n for all labels, responsive layout.

Tests:
- StorageBreakdownPanel renders chart with mock data
- StorageBreakdownPanel shows empty state when no data
- ApiUsagePanel renders table with usage bars
- ApiUsagePanel shows warning badge for high usage
</action>
<verify>
- Both panels render with mock data
- Donut chart visible in StorageBreakdownPanel
- Usage bars visible in ApiUsagePanel
- Tests pass
</verify>
<done>[ ]</done>
</task>

<task id="05-01-04">
<name>Create DeploymentTimeline and LimitExportButton</name>
<files>
- packages/webview/src/pages/Monitor/DeploymentTimeline.tsx
- packages/webview/src/pages/Monitor/DeploymentTimeline.test.tsx
- packages/webview/src/pages/Monitor/LimitExportButton.tsx
- packages/webview/src/pages/Monitor/LimitExportButton.test.tsx
</files>
<action>
**DeploymentTimeline:**
- Fetch data via useBridgeQuery('monitor:deployments')
- Use Timeline component (already exists in ui/) to display deployments
- Each entry: date, deployer name, status (success/failed/canceled), component count
- Badge for status: green=success, red=failed, gray=canceled
- Last 20 deployments
- Empty state: "No recent deployments"

**LimitExportButton:**
- Button component with download icon
- On click: gather current limits + trend data from useMonitorPageData
- Generate CSV: columns = Limit Name, Max, Remaining, Used %, Trend Direction
- Trigger browser download via Blob + URL.createObjectURL
- Show toast on success

Tests:
- DeploymentTimeline renders entries from mock data
- DeploymentTimeline shows empty state
- LimitExportButton generates CSV with correct headers
- LimitExportButton triggers download
</action>
<verify>
- Timeline renders deployment entries
- Export button generates CSV
- Tests pass
</verify>
<done>[ ]</done>
</task>

<task id="05-01-05">
<name>Integrate new panels into MonitorPage + i18n</name>
<files>
- packages/webview/src/pages/Monitor/MonitorPage.tsx
- packages/webview/src/pages/Monitor/MonitorPage.test.tsx
- packages/webview/src/pages/Monitor/useMonitorPageData.ts
- packages/webview/src/i18n/locales/en.json
- packages/webview/src/i18n/locales/fr.json
</files>
<action>
1. Read MonitorPage.tsx. Add new panels to the dashboard layout:
   - StorageBreakdownPanel after OrgInfo section
   - ApiUsagePanel after Governor Limits section
   - DeploymentTimeline after Jobs section
   - LimitExportButton in the Trends section header (next to period selector)

2. Update useMonitorPageData.ts if needed to expose new query hooks.

3. Add i18n keys to en.json and fr.json:
```
monitor.storage.title / monitor.storage.objects / monitor.storage.records / monitor.storage.percentage
monitor.deployments.title / monitor.deployments.empty / monitor.deployments.deployer / monitor.deployments.status
monitor.apiUsage.title / monitor.apiUsage.category / monitor.apiUsage.used / monitor.apiUsage.max / monitor.apiUsage.usage
monitor.export.button / monitor.export.success
```

4. Update MonitorPage.test.tsx: verify new panels render.

Tests:
- MonitorPage renders StorageBreakdownPanel
- MonitorPage renders DeploymentTimeline
- MonitorPage renders ApiUsagePanel
- LimitExportButton visible in Trends section
</action>
<verify>
- All 4 new elements visible in MonitorPage
- i18n keys in en.json and fr.json
- Tests pass
</verify>
<done>[ ]</done>
</task>

<task id="05-01-06">
<name>Run full validation</name>
<files>
- (none -- validation only)
</files>
<action>
```bash
pnpm typecheck
pnpm lint:fix
pnpm test
```
Fix any issues.
</action>
<verify>
- pnpm typecheck passes
- pnpm lint passes
- pnpm test passes
</verify>
<done>[ ]</done>
</task>

## Must-Haves

- [ ] Competitor benchmark in 05-BENCHMARK.md
- [ ] StorageBreakdownPanel with donut chart
- [ ] DeploymentTimeline with deployment entries
- [ ] LimitExportButton with CSV download
- [ ] ApiUsagePanel with usage breakdown
- [ ] monitor:health-score handler route registered
- [ ] New panels in MonitorPage layout
- [ ] All components have tests
- [ ] i18n keys in en.json and fr.json
- [ ] pnpm typecheck passes
- [ ] pnpm test passes
