---
wave: 1
depends_on: []
files_modified:
  - packages/webview/src/layouts/Sidebar/Sidebar.tsx
  - packages/webview/src/layouts/Sidebar/Sidebar.test.tsx
  - packages/webview/src/layouts/TopBar/TopBar.tsx
  - packages/webview/src/layouts/TopBar/TopBar.test.tsx
  - packages/webview/src/layouts/AppShell.tsx
  - packages/webview/src/layouts/StatusFooter/StatusFooter.tsx
  - packages/webview/src/layouts/StatusFooter/StatusFooter.test.tsx
  - packages/webview/vite.config.ts
  - packages/webview/src/bridge/BridgeProvider.tsx
  - packages/webview/src/bridge/BridgeProvider.test.tsx
autonomous: true
objective: "Fix layout UX: remove Grappe from sidebar, wire bell to notifications, dynamic version, auto-select org"
must_haves:
  - "Grappe hero button removed from sidebar navigation"
  - "Grappe route still accessible via direct URL"
  - "Bell button in TopBar opens NotificationCenter"
  - "Unread notification badge visible on bell button"
  - "StatusFooter version reads from build-time env, not hardcoded"
  - "First connected org auto-selected when no org is selected on mount"
  - "pnpm typecheck passes"
  - "pnpm test passes"
---

# Plan 01: Layout UX Fixes

## Objective

Fix 4 independent layout issues: remove Grappe from sidebar nav, wire the notifications bell, make version dynamic, and auto-select the first connected org.

## Context

- Grappe hero button is at Sidebar.tsx lines 214-230, needs removal but route stays
- Bell button in TopBar.tsx has no onClick handler, NotificationCenter is orphaned in AppShell
- StatusFooter.tsx hardcodes version '3.0.0'
- No auto-select logic exists when orgs load but nothing is selected
- Research Pitfall 2: use prop threading from AppShell to TopBar (matches existing pattern)
- Research Pitfall 3: auto-select in org:list:response listener, not a standalone useEffect
- Research Pitfall 1: Vite define needs JSON.stringify for string values

## Tasks

<task id="03-01-01">
<name>Remove Grappe from sidebar</name>
<files>
- packages/webview/src/layouts/Sidebar/Sidebar.tsx
- packages/webview/src/layouts/Sidebar/Sidebar.test.tsx
</files>
<action>
Read Sidebar.tsx. Remove:
1. Grappe hero button block (lines 214-230 area)
2. `grappe: Network` from iconMap (line 34 area)

Do NOT remove:
- `'grappe'` from router.tsx, useAppStore.ModuleRoute, or TopBar.ROUTE_LABELS
- GrappePage.tsx itself (stays for direct URL access)

Update Sidebar.test.tsx: remove any assertions about Grappe button rendering.
</action>
<verify>
- Grappe button not rendered in sidebar
- No TypeScript errors from removed references
- Grappe page still accessible via direct route
</verify>
<done>[ ]</done>
</task>

<task id="03-01-02">
<name>Wire bell button to NotificationCenter</name>
<files>
- packages/webview/src/layouts/TopBar/TopBar.tsx
- packages/webview/src/layouts/TopBar/TopBar.test.tsx
- packages/webview/src/layouts/AppShell.tsx
</files>
<action>
Read TopBar.tsx and AppShell.tsx.

1. Add `onNotificationsToggle?: () => void` prop to TopBar
2. In AppShell, pass `onNotificationsToggle={() => setNotificationsOpen(prev => !prev)}` to TopBar
3. In TopBar, wire bell button onClick to call `onNotificationsToggle`
4. Add unread badge: use `useNotificationStore(selectUnreadCount)` in TopBar
5. Render Badge component on bell button when unread count > 0

Update TopBar.test.tsx:
- Test: bell button calls onNotificationsToggle when clicked
- Test: unread badge shows count when notifications exist
- Test: no badge when unread count is 0
</action>
<verify>
- Bell button click toggles NotificationCenter
- Unread badge appears with correct count
- Tests pass
</verify>
<done>[ ]</done>
</task>

<task id="03-01-03">
<name>Dynamic version in StatusFooter</name>
<files>
- packages/webview/src/layouts/StatusFooter/StatusFooter.tsx
- packages/webview/src/layouts/StatusFooter/StatusFooter.test.tsx
- packages/webview/vite.config.ts
</files>
<action>
1. Read vite.config.ts. Add to existing `define` block:
   ```typescript
   __APP_VERSION__: JSON.stringify(require('./package.json').version)
   ```
   Or use `import { version } from './package.json'` if ESM.

2. Add TypeScript declaration for `__APP_VERSION__`:
   - Either in a `vite-env.d.ts` or inline `declare const __APP_VERSION__: string`

3. Read StatusFooter.tsx. Replace hardcoded `'3.0.0'` with `__APP_VERSION__`:
   ```typescript
   <span className="ml-auto opacity-70">{t('common.versionLabel', { version: __APP_VERSION__ })}</span>
   ```

4. Update StatusFooter.test.tsx:
   - Mock `__APP_VERSION__` via vitest globals or define
   - Update assertion from exact '3.0.0' to regex `/SandForge v\d+\.\d+\.\d+/` or mock value
</action>
<verify>
- StatusFooter renders version from build env
- No hardcoded version strings remain
- Test passes with mocked version
</verify>
<done>[ ]</done>
</task>

<task id="03-01-04">
<name>Auto-select first connected org on mount</name>
<files>
- packages/webview/src/bridge/BridgeProvider.tsx
- packages/webview/src/bridge/BridgeProvider.test.tsx
</files>
<action>
Read BridgeProvider.tsx. Find the `org:list:response` listener.

Add auto-select logic inside the listener callback, right after orgs are set:
```typescript
// Auto-select first connected org when none selected
const state = useOrgStore.getState();
if (!state.selectedOrgId && msg.payload.orgs.length > 0) {
  const connected = msg.payload.orgs.filter(o => o.status === 'connected');
  if (connected.length > 0) {
    state.selectOrg(connected[0].id);
  }
}
```

This fires only when fresh org data arrives, avoiding Pitfall 3 (race condition).

Update BridgeProvider.test.tsx:
- Test: auto-selects first connected org when none selected
- Test: does NOT auto-select when an org is already selected
- Test: does NOT auto-select when no connected orgs exist
</action>
<verify>
- First connected org auto-selected on mount
- No double-selection when org already selected
- Tests pass
</verify>
<done>[ ]</done>
</task>

<task id="03-01-05">
<name>Run full validation</name>
<files>
- (none -- validation only)
</files>
<action>
```bash
pnpm typecheck
pnpm lint:fix
pnpm test
```
Fix any issues.
</action>
<verify>
- pnpm typecheck passes
- pnpm lint passes
- pnpm test passes
</verify>
<done>[ ]</done>
</task>

## Must-Haves

- [ ] Grappe hero button removed from sidebar
- [ ] Grappe route still accessible via direct URL
- [ ] Bell button opens NotificationCenter
- [ ] Unread badge on bell button
- [ ] StatusFooter version from build env
- [ ] Auto-select first connected org on mount
- [ ] pnpm typecheck passes
- [ ] pnpm test passes
