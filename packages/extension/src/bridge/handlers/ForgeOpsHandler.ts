/**
 * @deprecated ForgeOpsHandler is no longer used.
 *
 * ForgeHandler now implements DomainHandler directly and is registered
 * in ExtensionHandlers.ts without a wrapper. This file is kept only
 * to avoid breaking any stale imports during the transition.
 *
 * Safe to delete once all references are confirmed removed.
 */
export {};
