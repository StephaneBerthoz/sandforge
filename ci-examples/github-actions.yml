# SandForge — GitHub Actions CI/CD Pipeline (EXAMPLE)
#
# Honest example built on what actually exists in this repo:
#   1. Quality gates   — pnpm validate (build:shared, typecheck, lint, tests, i18n, build)
#   2. Clone (dry-run) — packages/extension/cli/sandforge-clone.ts via tsx
#   3. Cleanup (dry-run) — packages/extension/cli/sandforge-cleanup.ts via tsx
#   4. VSIX package    — pnpm package, uploaded as an artifact
#
# There is NO `sandforge seed|sync|backup|health` CLI — those stages exist
# only in the VSCode extension UI. See ci-examples/README.md for details.
#
# Prerequisites for stages 2-3 (org-touching, dry-run by default):
#   - Repository secrets:
#     - SFDX_AUTH_URL_SOURCE: sfdx auth URL of the source org
#     - SFDX_AUTH_URL_TARGET: sfdx auth URL of the target sandbox
#   - Repository variables:
#     - SANDFORGE_SF_ORGS: set to 'true' to enable the clone/cleanup stages
#     - SF_CLONE_RECORD_ID: Salesforce record Id to clone (e.g. 500...)
#   - SLACK_WEBHOOK_URL (optional secret): failure notifications

name: SandForge Pipeline

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Run clone/cleanup in dry-run mode (no data written)'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
  schedule:
    - cron: '0 6 * * 1'

env:
  NODE_VERSION: '22'
  DRY_RUN: ${{ github.event.inputs.dry_run || 'true' }}

jobs:
  quality:
    name: Quality gates
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # `pnpm validate` chains build:shared → typecheck → lint → test →
      # check:i18n → build. Keeping it as one step is deliberate: build:shared
      # has to run first because every other package imports @sandforge/shared
      # from its dist/, and a bare `pnpm typecheck` on a fresh checkout dies
      # there before reaching a real error. Split the steps for finer reporting
      # only if `pnpm build:shared` stays in front.
      - name: Validate
        run: pnpm validate

  clone-dry-run:
    name: Forge clone (CLI)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: quality
    # Only runs when the repo admin has configured sf org secrets.
    if: vars.SANDFORGE_SF_ORGS == 'true'
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies and build shared
        run: |
          pnpm install --frozen-lockfile
          pnpm build:shared

      - name: Install Salesforce CLI
        run: npm install -g @salesforce/cli

      - name: Authenticate source and target orgs
        env:
          SFDX_AUTH_URL_SOURCE: ${{ secrets.SFDX_AUTH_URL_SOURCE }}
          SFDX_AUTH_URL_TARGET: ${{ secrets.SFDX_AUTH_URL_TARGET }}
        run: |
          echo "$SFDX_AUTH_URL_SOURCE" > /tmp/auth-source.txt
          echo "$SFDX_AUTH_URL_TARGET" > /tmp/auth-target.txt
          sf org login sfdxurl --sfdx-url-file /tmp/auth-source.txt --alias ci-source
          sf org login sfdxurl --sfdx-url-file /tmp/auth-target.txt --alias ci-target
          rm -f /tmp/auth-source.txt /tmp/auth-target.txt

      - name: Clone record graph (sandforge-clone)
        env:
          SF_CLONE_RECORD_ID: ${{ vars.SF_CLONE_RECORD_ID }}
        run: |
          DRY_FLAG=""
          [ "$DRY_RUN" = "true" ] && DRY_FLAG="--dry-run"
          pnpm exec tsx packages/extension/cli/sandforge-clone.ts \
            --record "$SF_CLONE_RECORD_ID" \
            --source ci-source \
            --target ci-target \
            --upsert \
            $DRY_FLAG \
            --json \
            --remap-csv remap.csv \
            > clone-summary.json

      - name: Cleanup cloned records (sandforge-cleanup, dry-run)
        run: |
          DRY_FLAG=""
          [ "$DRY_RUN" = "true" ] && DRY_FLAG="--dry-run"
          pnpm exec tsx packages/extension/cli/sandforge-cleanup.ts \
            --target ci-target \
            --since today \
            $DRY_FLAG

      - name: Upload clone artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: clone-report
          path: |
            clone-summary.json
            remap.csv
          retention-days: 30

  package-vsix:
    name: Package VSIX
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: quality
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build and package VSIX
        run: pnpm package

      - name: Upload VSIX
        uses: actions/upload-artifact@v4
        with:
          name: sandforge-vsix
          path: sandforge.vsix
          retention-days: 30

  notify:
    name: Notify on failure
    runs-on: ubuntu-latest
    needs: [quality, clone-dry-run, package-vsix]
    if: failure()
    steps:
      - name: Post to Slack
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "No SLACK_WEBHOOK_URL secret configured, skipping notification"
            exit 0
          fi
          curl -X POST "$SLACK_WEBHOOK_URL" \
            -H 'Content-Type: application/json' \
            -d "{\"text\": \"SandForge Pipeline FAILED on ${{ github.repository }} (${{ github.ref_name }}) — ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"}"
